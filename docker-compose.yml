services:
  api:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: projr-api
    env_file:
      - .env.example
    environment:
      DATABASE_URL: postgresql+psycopg://projr:projr@db:5432/projr
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped

  db:
    image: postgres:15-alpine
    container_name: projr-db
    environment:
      POSTGRES_DB: projr
      POSTGRES_USER: projr
      POSTGRES_PASSWORD: projr
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U projr -d projr"]
      interval: 5s
      timeout: 5s
      retries: 10
    restart: unless-stopped

volumes:
  postgres_data:
